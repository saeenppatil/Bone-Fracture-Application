import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Bone Fracture Application',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
